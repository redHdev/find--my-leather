import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin Page",
  description: "Find My Leather - Admin Page",
  // other metadata
};

const AdminPage = () => {
  return <div>AdminPage</div>;
};

export default AdminPage;
